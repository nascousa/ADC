version: '3.8'

services:
  mailhog:
    image: mailhog/mailhog:latest
    container_name: mailhog-local
    restart: unless-stopped
    ports:
      - '1025:1025'
      - '8025:8025'
    networks:
      - mail-network

networks:
  mail-network:
    driver: bridge
