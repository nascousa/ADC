version: '3.8'

services:
  frontend:
    image: your-refine-antd-frontend-image:latest
    container_name: refine-antd-frontend
    restart: unless-stopped
    ports:
      - '3030:3000'
    environment:
      - NODE_ENV=production
      - VITE_API_BASE_URL=http://api:8000
    depends_on:
      api:
        condition: service_healthy
    healthcheck:
      test: ['CMD-SHELL', 'wget --no-verbose --tries=1 --spider http://127.0.0.1:3000 || exit 1']
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
    networks:
      - refine-stack-network

  api:
    image: your-fastapi-image:latest
    container_name: refine-fastapi-api
    restart: unless-stopped
    expose:
      - '8000'
    environment:
      - ENVIRONMENT=production
      - DATABASE_URL=postgresql://app:app@postgres:5432/refinedb
      - REDIS_URL=redis://redis:6379/0
      - VECTOR_DB_URL=postgresql://app:app@postgres:5432/refinedb
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      pgvector-init:
        condition: service_completed_successfully
    command: ['uvicorn', 'app.main:app', '--host', '0.0.0.0', '--port', '8000', '--workers', '2']
    healthcheck:
      test: ['CMD-SHELL', 'curl -fsS http://localhost:8000/health || exit 1']
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
    volumes:
      - refine_fastapi_data:/app/data
    networks:
      - refine-stack-network

  postgres:
    image: pgvector/pgvector:pg16
    container_name: refine-pgvector
    restart: unless-stopped
    environment:
      - POSTGRES_DB=refinedb
      - POSTGRES_USER=app
      - POSTGRES_PASSWORD=app
    volumes:
      - refine_pgvector_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U app -d refinedb']
      interval: 10s
      timeout: 5s
      retries: 6
    networks:
      - refine-stack-network

  pgvector-init:
    image: pgvector/pgvector:pg16
    container_name: refine-pgvector-init
    restart: 'no'
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      - PGPASSWORD=app
    entrypoint:
      - /bin/sh
      - -c
      - |
        psql -h postgres -U app -d refinedb -c "CREATE EXTENSION IF NOT EXISTS vector;"
    networks:
      - refine-stack-network

  redis:
    image: redis:7-alpine
    container_name: refine-redis
    restart: unless-stopped
    command: ['redis-server', '--appendonly', 'yes']
    volumes:
      - refine_redis_data:/data
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 10s
      timeout: 3s
      retries: 5
    networks:
      - refine-stack-network

volumes:
  refine_fastapi_data:
    driver: local
  refine_pgvector_data:
    driver: local
  refine_redis_data:
    driver: local

networks:
  refine-stack-network:
    driver: bridge
