version: '3.8'

services:
  api:
    image: your-express-image:latest
    container_name: node-express-api
    restart: unless-stopped
    ports:
      - '8001:8000'
    environment:
      - NODE_ENV=production
      - PORT=8000
      - DATABASE_URL=postgresql://app:app@postgres:5432/appdb
      - REDIS_URL=redis://redis:6379/0
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ['CMD', 'wget', '--no-verbose', '--tries=1', '--spider', 'http://127.0.0.1:8000/health']
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
    networks:
      - node-network

  postgres:
    image: postgres:16-alpine
    container_name: node-express-postgres
    restart: unless-stopped
    environment:
      - POSTGRES_DB=appdb
      - POSTGRES_USER=app
      - POSTGRES_PASSWORD=app
    volumes:
      - node_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U app -d appdb']
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - node-network

  redis:
    image: redis:7-alpine
    container_name: node-express-redis
    restart: unless-stopped
    command: ['redis-server', '--appendonly', 'yes']
    volumes:
      - node_redis_data:/data
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 10s
      timeout: 3s
      retries: 5
    networks:
      - node-network

volumes:
  node_postgres_data:
    driver: local
  node_redis_data:
    driver: local

networks:
  node-network:
    driver: bridge
